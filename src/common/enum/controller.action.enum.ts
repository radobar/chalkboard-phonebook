export enum ControllerActionEnum {
  // todo not necessary? might keep
  AuthSignin = 'Login in the system',
  AuthSignup = 'Registration in the system',
  AuthGetIp = 'Getting the IP address of the current user',
  ContactGetById = 'Getting a contact by id',
  ContactList = 'Listing contacts',
  ContactUpdate = 'Contact update',
  ContactCreate = 'Contact Create',
  ContactDeleteById = 'Delete contact by id',
}
